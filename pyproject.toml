[project]
name = "delivery-ai-agent"
version = "0.1.0"
description = "A conversational AI agent for delivery tracking using ElevenLabs Conversational AI"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "elevenlabs[pyaudio]>=2.34.0",
    "python-dotenv>=1.2.1",
]
